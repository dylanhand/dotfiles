alt - a : open /Applications/Claude.app
alt - s : open /Applications/Spotify.app
alt - d : open /Applications/Day\ One.app
alt - f : open /Applications/Firefox.app
alt - g : open /Applications/Fork.app
# alt - 0x29 : open /Applications/Fork.app  # 0x29 = ;
alt - j : open /Applications/Logic\ Pro\ X.app
alt - k : open /Applications/Visual\ Studio\ Code.app
alt - h : open /Applications/Toggl\ Track.app
alt - l : open /Applications/iTerm.app
alt - w : open /Applications/WhatsApp.app
alt - e : open ~/Applications/Gmail.app
alt - t : open /Applications/Things3.app
alt - r : open /System/Library/CoreServices/Finder.app
alt - m : open /System/Applications/Messages.app
alt - n : open /System/Applications/Notes.app
alt - u : open /Applications/Universal\ Audio/Console.app
alt - i : open /Applications/Google\ Chrome.app
alt - o : open /Applications/Obsidian.app
alt - p : open /Applications/1Password.app
alt - z : open /Applications/Zulip.app
alt - x : open /Applications/Xcode.app
alt - c : open /System/Applications/Calendar.app
alt - v : open /Applications/Setapp/ClearVPN.app

# trying to get kj to escape in Xcode vim mode
# alt - b : skhd --key 0x35

# change window focus within space
# alt - j : yabai -m window --focus south
# alt - k : yabai -m window --focus north
# alt - h : yabai -m window --focus west
# alt - l : yabai -m window --focus east

# focus display
# ctrl + alt - h : yabai -m display --focus west;
# ctrl + alt - l : yabai -m display --focus east;
# ctrl + alt - j : yabai -m display --focus south;
# ctrl + alt - k : yabai -m display --focus north;

# rotate layout clockwise
# shift + alt - r : yabai -m space --rotate 270

# flip along y-axis
# shift + alt - y : yabai -m space --mirror y-axis

# flip along x-axis
# shift + alt - x : yabai -m space --mirror x-axis

# toggle window float
# shift + alt - t : yabai -m window --toggle float --grid 4:4:1:1:2:2

# maximize a window
# shift + alt - m : yabai -m window --toggle zoom-fullscreen

# balance out tree of windows (resize to occupy same area)
# shift + alt - e : yabai -m space --balance

# swap windows
# shift + alt - j : yabai -m window --swap south
# shift + alt - k : yabai -m window --swap north
# shift + alt - h : yabai -m window --swap west
# shift + alt - l : yabai -m window --swap east

# move window and split
# ctrl + alt - j : yabai -m window --warp south
# ctrl + alt - k : yabai -m window --warp north
# ctrl + alt - h : yabai -m window --warp west
# ctrl + alt - l : yabai -m window --warp east

# move window to display left and right
# ctrl + shift + alt - h : yabai -m window --display west; yabai -m display --focus west;
# ctrl + shift + alt - l : yabai -m window --display east; yabai -m display --focus east;
# ctrl + shift + alt - j : yabai -m window --display south; yabai -m display --focus south;
# ctrl + shift + alt - k : yabai -m window --display north; yabai -m display --focus north;

#move window to prev and next space
# shift + alt - p : yabai -m window --space prev;
# shift + alt - n : yabai -m window --space next;

# move window to space #
# shift + alt - 1 : yabai -m window --space 1;
# shift + alt - 2 : yabai -m window --space 2;
# shift + alt - 3 : yabai -m window --space 3;
# shift + alt - 4 : yabai -m window --space 4;
# shift + alt - 5 : yabai -m window --space 5;
# shift + alt - 6 : yabai -m window --space 6;
# shift + alt - 7 : yabai -m window --space 7;
